<h3>タグリスト</h3>
<% @tag_list.each do |list| %>
  <%= list.tag_name %>
  <%= "(#{list.genres.count})" %>
<% end %>
<br>
<%= "#{@tag.tag_name}" %>
<br>
<% @genres.each do |genre| %>
  <%= link_to genre.content, genre %>
<% end %>