<div class="kensaku">
  <div class="kensaku2">
    <%= form_tag({controller:"points",action:"index"}, method: :get) do %>
      <%= text_field_tag :search %>
      <%= submit_tag '検索する'  %>
    <% end %>
  </div>
</div>


<div class="tag">
  <h3>タグリスト</h3>
    <div class="tag-box">
      <% @tag_list.each do |list| %>
        <div class="tag-list">
          #<%= link_to list.tag_name, tag_points_path(tag_id: list.id) %>
          <%= "(#{list.points.count})" %>
        </div>
      <% end %>
    </div>
</div>

