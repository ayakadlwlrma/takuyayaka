<h3>タグリスト</h3>
<% @tag_list.each do |list| %>
    <%= list.tag_name %>
    <%= "(#{list.points.count})" %>
<% end %>
<br>
<%= "#{@tag.tag_name}" %>
<br>
<% @points.each do |point| %>
    <%= link_to point.content, point %>
<% end %>