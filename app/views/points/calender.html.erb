<p id="notice"><%= notice %></p>

<h1>自分の日記</h1>

<br>

<%= month_calendar events: @points do |date, points| %>
  <%= date.day %>

  <% points.each do |point| %>
    <div>
      <%= link_to point.song, point %>
    </div>
  <% end %>
<% end %>